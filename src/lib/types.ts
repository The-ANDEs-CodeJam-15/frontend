export type Player = { userName: string; sessionID: string; points: number, color: string };
export type Curse = { curseIndex: number; curseName: string };
export type Song = { trackID: number; name: string; artist: string; cover: string };

